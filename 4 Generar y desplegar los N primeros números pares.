#include <iostream>

using namespace std;

int main() {
    int N;

    cout << "Ingrese el valor de N: ";
    cin >> N;

    cout << "Los primeros " << N << " números pares son: ";
    for (int i = 2; i <= 2 * N; i += 2) {
        cout << i << " ";
    }
    cout << endl;

    return 0;
}
